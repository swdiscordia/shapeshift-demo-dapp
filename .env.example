# Required AFTER running `integrate shapeshift` if you want affiliate fee
# attribution on the swaps you generate. Create a partner code at
# https://dashboard.affiliate.shapeshift.com/ — it's free, takes 1 minute.
# Without a code the integration still works; the API will just not attribute
# fees to you (you get the default unattributed BPS).
SHAPESHIFT_PARTNER_CODE=
